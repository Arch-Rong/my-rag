# ---------------------------------------------------------------------------
# 常量：不建表，只给代码和数据库迁移共用同一个数字
# ---------------------------------------------------------------------------

# 每条 chunk 的 embedding 向量长度（维数）
# 必须和 .env 里 EMBEDDING_DIM、Alembic 迁移里 Vector(1024) 一致
# 换 Embedding 模型（如 bge-m3）时要一起改，并重新生成向量
DEFAULT_EMBEDDING_DIM = 1024
