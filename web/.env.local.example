# 前端访问后端的根路径（浏览器侧）
# 开发推荐 /backend，由 next.config.ts 代理到 http://127.0.0.1:8000
NEXT_PUBLIC_API_BASE_URL=/backend

# 若不用代理、直连 FastAPI（需 server 开启 CORS）可改为：
# NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000

# Next 服务端代理目标（仅 rewrites 使用，不要加 NEXT_PUBLIC_）
API_PROXY_TARGET=http://127.0.0.1:8000
